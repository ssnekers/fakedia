services:
  - type: web
    name: telegram-bot
    runtime: runtime: python-3.13.12
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
